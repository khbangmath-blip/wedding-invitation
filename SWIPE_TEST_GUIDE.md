# 스와이프 갤러리 테스트 가이드

## 구현 내용
갤러리 모달에 **터치 스와이프** 기능이 추가되었습니다.

### 추가된 기능
1. **좌측 스와이프** → 다음 이미지 (기존 우측 화살표 버튼과 동일)
2. **우측 스와이프** → 이전 이미지 (기존 좌측 화살표 버튼과 동일)
3. 기존 기능 유지:
   - 좌우 화살표 버튼
   - 키보드 화살표 키
   - 마우스 클릭

### 스와이프 감지 설정
- **최소 스와이프 거리**: 50px
- **이동 감지 임계값**: 10px (클릭과 구분)
- 수직 스크롤과 수평 스와이프 자동 구분

## 테스트 방법

### 1. 개발 서버 시작
```bash
npm start
```
브라우저에서 `localhost:3000`이 자동 열림

### 2. 모바일/태블릿 테스트 (권장)
#### Chrome 개발자 도구로 시뮬레이션:
1. F12 개발자 도구 열기
2. Ctrl+Shift+M (또는 Toggle device toolbar 클릭)
3. 기기 선택 (iPhone 12, iPad 등)
4. 갤러리 클릭 → 이미지 확대 모달 열기
5. 이미지 위에서 **좌측/우측으로 드래그**

#### 실제 모바일 기기:
1. 개발 서버 실행 후 로컬 네트워크 IP로 접속
2. 갤러리 이미지 클릭
3. 화면에서 좌우로 스와이프

### 3. 테스트 항목

#### ✅ 정상 동작 확인
- [ ] 갤러리 썸네일 클릭 → 모달 열림
- [ ] 좌측 스와이프 → 다음 이미지로 이동
- [ ] 우측 스와이프 → 이전 이미지로 이동
- [ ] 마지막 이미지에서 좌측 스와이프 → 첫 번째 이미지 (순환)
- [ ] 첫 번째 이미지에서 우측 스와이프 → 마지막 이미지 (순환)
- [ ] 왼쪽 화살표 버튼 클릭 → 여전히 작동
- [ ] 오른쪽 화살표 버튼 클릭 → 여전히 작동
- [ ] 키보드 화살표 키 → 여전히 작동
- [ ] 수직 스크롤 → 스와이프로 간주되지 않음
- [ ] 작은 드래그 (50px 미만) → 무시됨 (클릭으로 간주)

#### ⚠️ 버그 방지 확인
- [ ] 빠르게 연속 스와이프 → 부드럽게 처리
- [ ] 대각선 드래그 → 주로 수평 이동만 감지
- [ ] 이미지 로딩 중 스와이프 → 정상 작동
- [ ] 모달 바깥 클릭 후 스와이프 → 작동 안 함

### 4. 문제 발생 시 확인사항

#### 스와이프가 작동 안 할 경우:
```javascript
// 브라우저 콘솔에서 확인
document.querySelector('[data-gallery-modal]') // 모달 요소 존재 확인
```

#### 수직 스크롤 막힘:
- `touchAction: 'pan-y pinch-zoom'` 설정 확인
- 모바일 브라우저 설정에서 터치 제스처 확인

#### 화살표 버튼 경쟁:
- 버튼 영역에서의 스와이프는 버튼 클릭 우선
- 화면 중앙에서 스와이프 테스트

## 스와이프 로직 상세

```
터치 시작 (X 좌표 기록)
    ↓
터치 이동 (수적/수평 구분)
    ├─ 수평: preventDefault 실행
    └─ 수직: 스크롤 허용
    ↓
터치 종료
    ├─ |X_시작 - X_종료| > 50px 이면
    │   ├─ X_시작 > X_종료 (좌측 스와이프) → 다음 이미지
    │   └─ X_시작 < X_종료 (우측 스와이프) → 이전 이미지
    └─ 아니면 무시
```

## 성능 최적화
- 이미지 미리 로드 (다음/이전)
- 불필요한 리렌더링 방지
- 터치 이벤트 한 번만 등록

---
**테스트 후 이상 사항 발견 시, 콘솔 개발자 도구의 Console 탭에서 에러 메시지 확인해주세요.**
